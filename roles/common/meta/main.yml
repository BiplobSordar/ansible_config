---
galaxy_info:
  role_name: common
  author: Platform Engineering
  description: >
    Verifies (never installs) OS, kernel, swap, sysctl, chrony, Python and
    required package prerequisites baked into the Golden AMI.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - "26.04"
dependencies: []
