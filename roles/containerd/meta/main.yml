---
galaxy_info:
  role_name: containerd
  author: Platform Engineering
  description: >
    Verifies the pre-installed containerd runtime (version, service state,
    SystemdCgroup) and ensures /etc/crictl.yaml exists. Never installs containerd.
  license: Apache-2.0
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - "26.04"
dependencies: []
