---
# roles/kubernetes/defaults/main.yml

kubernetes_expected_version: "{{ kubernetes_version | default('1.30.4') }}"

kubernetes_binary_path: /usr/bin

kubelet_service_name: kubelet

kubernetes_held_packages_common:
  - kubeadm
  - kubelet

kubernetes_held_packages_control_plane_extra:
  - kubectl